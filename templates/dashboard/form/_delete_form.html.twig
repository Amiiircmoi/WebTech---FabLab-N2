<form method="post" action="{{ path('app_dashboard_form_delete', {'id': form.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cette demande ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form.id) }}">
	<button title="Supprimer" class="btn btn-danger">
		<i class="fa-solid fa-trash"></i>
		Supprimer
	</button>
</form>
